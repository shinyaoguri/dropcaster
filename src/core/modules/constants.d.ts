export const PREVIEW_OPTIONS: {
  width: number;
  height: number;
  duration: number;
  fps: number;
  quality: number;
  maxColors: number;
  scaleFilter: string;
};

export const DEFAULT_DESCRIPTION_SUFFIX: string;
export const DEFAULT_PATH_PREFIX: string;
export const PREVIEW_PATH_PREFIX: string;

export const MANUAL_METADATA_FILE: string;
export const MANUAL_METADATA_TEMPLATE_FILE: string;

export const API_CONFIG: {
  baseUrl: string;
  apiBaseUrl: string;
  userAgent: string;
  apiRequestTimeout: number;
  apiRequestIntervalMs: number;
};

export const DEFAULTS: {
  unknownUser: string;
  unknownTitle: string;
};
