{
  "name": "dropcaster-asset-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies OpenProcessing CDN with CORS headers added, so dropcaster viewer can captureStream sketches that load external assets.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^6.0.3",
    "wrangler": "^3.90.0"
  }
}
